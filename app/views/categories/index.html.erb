<h1>All Categories</h1>

<div><%= link_to 'Add a New Category', new_category_path %></div>

<ul>
<% @categories.each do |c|%>
    <li><%= link_to c.name, category_path(c) %> - <%= c.parties.size %></li>
<% end %>
</ul>